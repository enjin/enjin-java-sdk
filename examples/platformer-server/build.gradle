dependencies {
    implementation deps.websocket
    implementation deps.gson
    implementation project(':sdk')
}

jar {
    manifest {
        attributes(
                'Main-Class': 'com.enjin.platformer.server.App'
        )
    }
}
