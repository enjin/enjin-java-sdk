query getAllUsers {
    EnjinUsers(%s) {
        id,
        name,
        email,
        roles {
            id,
            name,
            permissions {
                id,
                name
            },
            app_id
        },
        created_at,
        updated_at,
        identities {
            id,
            app_id,
            ethereum_address,
            linking_code,
            fields {
                key,
                value
            }
        }
    }
}