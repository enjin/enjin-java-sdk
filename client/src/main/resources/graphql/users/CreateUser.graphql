mutation createUser {
    CreateEnjinUser(%s) {
        id,
        name,
        email,
        roles {
            id,
            name,
            permissions,
            app_id
        },
        access_tokens,
        created_at,
        updated_at,
        identities {
            id,
            app_id,
            ethereum_address,
            linking_code,
            fields {
                key,
                value
            }
        }
    }
}