query login {
    EnjinOauth(%s) {
        id,
        name,
        email,
        roles {
            id,
            name,
            permissions,
            app_id
        },
        access_tokens,
        created_at,
        updated_at
    }
}