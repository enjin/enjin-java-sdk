query getAllUsers {
    EnjinUsers {
        id,
        name,
        email,
        roles {
            id,
            name,
            permissions,
            app_id
        },
        created_at,
        updated_at
    }
}