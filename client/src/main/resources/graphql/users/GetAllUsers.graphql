query getAllUsers {
    EnjinUsers {
        id,
        name,
        email,
        roles {
            id,
            name,
            permissions,
            app_id
        },
        created_at,
        updated_at,
        identities {
            id,
            app_id,
            ethereum_address,
            linking_code,
            fields {
                key,
                value
            }
        }
    }
}