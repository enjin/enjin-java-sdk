mutation createIdentity {
    CreateEnjinIdentity(%s) {
        id,
        name,
        email,
        roles {
            id,
            name,
            app_id
        },
        access_tokens,
        created_at,
        updated_at
    }
}