mutation createUser {
    CreateEnjinUser(%s) {
        id,
        name,
        email,
        roles {
            id,
            name,
            permissions {
                id,
                name
            },
            app_id
        },
        access_tokens,
        identities {
            id,
            app_id,
            ethereum_address,
            eth_balance,
            enj_balance,
            linking_code,
            fields {
                key,
                value
            },
            tokens {
                token_id,
                balance
            }
        }
    }
}