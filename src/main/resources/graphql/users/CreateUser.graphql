mutation createUser {
    CreateEnjinUser(%s) {
        id,
        name,
        email,
        roles {
            id,
            name,
            permissions {
                id,
                name
            },
            app_id
        },
        access_tokens,
        identities {
            id,
            app_id,
            ethereum_address,
            linking_code,
            linking_code_qr,
            fields {
                key,
                value
            },
            tokens {
                token_id,
                balance
            },
            eth_balance,
            enj_balance,
            enj_allowance,
            all_tokens_balance
        }
    }
}