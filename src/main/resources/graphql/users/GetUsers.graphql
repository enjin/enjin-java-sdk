query getUsers {
    EnjinUsers(%s) {
        id,
        name,
        email,
        access_tokens,
        roles {
            id,
            name,
            permissions {
                id,
                name
            },
            app_id
        }
    }
}