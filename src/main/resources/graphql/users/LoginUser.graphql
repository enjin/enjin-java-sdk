query login {
    EnjinOauth(%s) {
        id,
        name,
        email,
        access_tokens,
        roles {
            id,
            name,
            permissions {
                id,
                name
            },
            app_id
        },
        identities {
            id,
            app_id,
            ethereum_address,
            linking_code,
            linking_code_qr,
            fields {
                key,
                value
            },
            tokens {
                token_id,
                balance
            },
            eth_balance,
            enj_balance,
            enj_allowance,
            all_tokens_balance
        }
    }
}